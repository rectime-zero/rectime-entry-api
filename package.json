{
  "name": "rectime-entry-api",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:wrangler-toml && wrangler dev --config wrangler.toml",
    "generate:wrangler-toml": "node ./scripts/generate-wrangler-toml.mjs",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}